
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-expect-error
export default function NavbarIcon({icon}) {
    return (
        <div className="sidebar-icon left-full group">
            {icon}
        </div>
    );
}

